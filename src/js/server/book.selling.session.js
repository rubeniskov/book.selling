(function( bs ){
    
    var swig        = require('mongodb'),

        cheerio     = require("cheerio"),

        fs          = require("fs"),

        path        = require("path");

    bs.session      = function( res, req, next )
    {
        
    }

    bs.session.store= {};
    
})( BookSelling );